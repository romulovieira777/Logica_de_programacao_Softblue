início {
    // Criando um tabuada
    
    inteiro: t;
    inteiro: n;
    
    leia("Tabuada do...", t);
    
    para n de 1 até 10 passo 1 {
        escreva(t + " x " + n + " = " + (t * n));
    }         
}